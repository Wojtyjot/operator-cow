seed: ${generate_random_seed:}

data_dir: ${hydra:runtime.cwd}/data

outputs_dir: ${hydra:runtime.cwd}/outputs

outputs_subdir: dev

job_subdir: dev

hydra:
  run:
    dir: outputs/${outputs_subdir}/${hydra.job.name}/${job_subdir}/${now:%Y-%m-%d_%H-%M-%S-%f}
  job:
    chdir: true
  verbose: false

wandb:
  project: operator-cow
  dir: ${hydra:runtime.cwd}/outputs
  mode: online
  anonymous: allow
  tags:
    - development
data:
  path_train: /home/wssk-ptw/Operator/COW_DATASET/GNOT_train_new_BC_A_out.npy
  path_test: /home/wssk-ptw/Operator/COW_DATASET/GNOT_test_new_BC_A_out.npy
  batch_size: 32

model:
  name: VANO
  layers_encoder: [111, 256, 256, 256, 32]
  layers_decoder: [27, 256, 256, 256, 100]
  model_save_path: /home/wssk-ptw/Operator/COW_DATASET
  latent_dim: 16

training:
  epochs: 500
  grad_clip: 1000.0
  S: 16
  beta: 0.001


optimizer:
  lr: 0.0001
  weight_decay: 0.00005
  beta1: 0.9
  beta2: 0.999
